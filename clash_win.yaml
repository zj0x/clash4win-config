# ============================================================
# Clash Verge Rev 配置 (Mihomo 内核)
# 适配：Windows | 地区：中国大陆 | 模式：白名单
# 白名单逻辑：已知国内域名/IP → 直连，其余 → 代理（安全优先）
# 更新：2026-02-27
# ============================================================

# ─── 基础设置 ───────────────────────────────────────────────
mixed-port: 7890                    # HTTP + SOCKS5 混合端口
allow-lan: true                     # 允许局域网设备连接
bind-address: "*"                   # 监听所有网卡
mode: rule                          # 规则模式
log-level: info                     # 日志级别
ipv6: false                         # 关闭 IPv6（国内 IPv6 环境复杂，易泄漏）
external-controller: 127.0.0.1:9090 # 面板控制端口

# ─── 内核性能优化 ───────────────────────────────────────────
unified-delay: true                 # 统一延迟计算，结果更准确
tcp-concurrent: true                # TCP 并发连接，自动选最快
keep-alive-interval: 30             # 保活间隔(秒)，防 NAT 超时断连
find-process-mode: strict           # 精确进程匹配
global-client-fingerprint: chrome   # TLS 指纹伪装为 Chrome，降低特征检测风险

# ─── GeoData 配置 ──────────────────────────────────────────
geodata-mode: true                  # 使用 .dat 格式（v2ray 兼容，更全）
geodata-loader: standard            # standard 加载更快（内存充足时推荐）
geo-auto-update: true               # 自动更新 GeoData
geo-update-interval: 24             # 更新间隔(小时)

# GeoData 下载源（使用 jsdelivr CDN，国内访问快）
geox-url:
  geoip: "https://cdn.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/geoip-lite.dat"
  geosite: "https://cdn.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/geosite.dat"
  mmdb: "https://cdn.jsdelivr.net/gh/MetaCubeX/meta-rules-dat@release/country-lite.mmdb"

# ─── 持久化 ────────────────────────────────────────────────
profile:
  store-selected: true              # 记住策略组手动选择
  store-fake-ip: true               # 缓存 Fake-IP 映射，重启不丢失

# ─── TUN 虚拟网卡（接管全部系统流量）─────────────────────────
tun:
  enable: true
  stack: mixed                      # gVisor + System 混合栈，Windows 推荐
  dns-hijack:
    - "any:53"                      # 劫持所有 DNS，防止泄漏
  auto-route: true
  auto-detect-interface: true
  mtu: 1500
  strict-route: true                # 严格路由，防流量绕过 TUN
  endpoint-independent-nat: true    # 改善 NAT 穿透（游戏、语音）

# ─── 流量嗅探 ──────────────────────────────────────────────
# 嗅探 TLS/HTTP/QUIC 中的域名，辅助分流（解决纯 IP 连接分流失败）
sniffer:
  enable: true
  force-dns-mapping: true
  parse-pure-ip: true
  override-destination: true
  sniff:
    TLS:
      ports: [443, 8443]
    HTTP:
      ports: [80, 8080-8880]
    QUIC:
      ports: [443, 8443]
  skip-domain:
    - "Mijia Cloud"
    - "+.push.apple.com"

# ─── DNS 配置（核心防污染）─────────────────────────────────
# 策略：fake-ip + respect-rules
#   • 国内域名 → 国内 DoH 解析 → 直连
#   • 被墙域名 → 返回 fake-ip → 由代理远端解析（零 DNS 泄漏）
#   • 无需配置海外 DNS：fake-ip 模式下被墙域名根本不走本地 DNS
dns:
  enable: true
  listen: 127.0.0.1:1053
  ipv6: false

  # 模式
  enhanced-mode: fake-ip
  fake-ip-range: 198.18.0.1/16

  # 性能
  cache-algorithm: arc              # ARC 缓存，命中率优于 LRU
  use-hosts: true
  use-system-hosts: true
  respect-rules: true               # DNS 解析遵循分流规则

  # 引导 DNS（纯 IP，解析 DoH 域名本身，打破鸡生蛋问题）
  default-nameserver:
    - 223.5.5.5                     # 阿里 DNS
    - 119.29.29.29                  # 腾讯 DNSPod

  # 代理节点域名解析（走国内 UDP DNS，低延迟）
  proxy-server-nameserver:
    - 223.5.5.5
    - 119.29.29.29

  # 主 DNS（未命中 nameserver-policy 的域名使用）
  nameserver:
    - https://doh.pub/dns-query
    - https://dns.alidns.com/dns-query

  # 域名策略 DNS — 仅配置国内域名 → 国内 UDP DNS（低延迟）
  # 被墙域名无需配置：fake-ip 直接返回假 IP，由代理远端解析
  nameserver-policy:
    "geosite:cn,geolocation-cn,private,apple-cn,apple@cn,steam@cn,category-games@cn":
      - 223.5.5.5
      - 119.29.29.29

  # Fake-IP 过滤（以下域名返回真实 IP，不走 fake-ip）
  # 解决：Windows "无网络"图标、STUN 穿透、游戏联机、NTP 等问题
  fake-ip-filter:
    # Windows 网络探测
    - "+.msftconnecttest.com"
    - "+.msftncsi.com"
    - "dns.msftncsi.com"
    # NTP 时间同步
    - "+.ntp.org.cn"
    - "+.pool.ntp.org"
    - "time.*.com"
    - "time.*.gov"
    - "time.*.edu.cn"
    - "time.*.apple.com"
    - "ntp.*.com"
    # 局域网 / 本地
    - "+.lan"
    - "+.local"
    - "+.localhost"
    # STUN / NAT 穿透 / WebRTC
    - "+.stun.*.*"
    - "+.stun.*"
    - "stun.*.*"
    - "stun.*"
    - "+.turn.twilio.com"
    # QQ 登录
    - "localhost.ptlogin2.qq.com"
    - "localhost.sec.qq.com"
    # 网易云音乐（需要真实 IP 才能正常播放）
    - "+.music.163.com"
    - "music.126.net"
    # 游戏平台
    - "+.battlenet.com.cn"
    - "+.logon.battlenet.com.cn"
    - "+.logon.battle.net"
    - "+.srv.nintendo.net"
    - "+.stun.playstation.net"
    - "xbox.*.microsoft.com"
    - "+.xboxlive.com"
    - "+.market.xboxcn.com"
    # 网络门户检测
    - "captive.apple.com"
    - "connectivitycheck.gstatic.com"
    - "detectportal.firefox.com"
    - "resolver1.opendns.com"
    - "wifi.vivo.com.cn"
    # Google Lens
    - "lens.l.google.com"

# ─── 代理提供者 ────────────────────────────────────────────
# 🔴 请将 url 替换为你的机场订阅链接
proxy-providers:
  MyAirport:
    type: http
    url: "https://example.com/api/v1/client/subscribe?token=YOUR_TOKEN"
    interval: 3600
    path: ./proxies/airport.yaml
    # 过滤信息节点（取消注释按需启用）
    # exclude-filter: "(?i)(到期|剩余|流量|官网|套餐|重置)"
    health-check:
      enable: true
      interval: 300
      url: http://cp.cloudflare.com/generate_204
      lazy: true
    override:
      client-fingerprint: chrome
      skip-cert-verify: true
      udp: true

# ─── 策略组 ────────────────────────────────────────────────
proxy-groups:

  # ── 核心策略 ──

  - name: "🚀 节点选择"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Rocket.png
    proxies:
      - "⚡ 自动选择"
      - "🔧 手动切换"
      - "香港"
      - "台湾"
      - "日本"
      - "新加坡"
      - "美国"
      - "DIRECT"

  - name: "⚡ 自动选择"
    type: url-test
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Auto.png
    use:
      - MyAirport
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    tolerance: 80
    lazy: true

  # ── 专用策略 ──

  # AI 服务（ChatGPT / Claude / Gemini / Copilot 等）
  # 注意：OpenAI 封禁香港 IP，Claude 部分地区不可用，建议美国/日本/新加坡
  - name: "🧠 AI 服务"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Bot.png
    proxies:
      - "美国"
      - "日本"
      - "新加坡"
      - "台湾"
      - "⚡ 自动选择"
      - "🔧 手动切换"

  - name: "🔍 Google"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Google_Search.png
    proxies:
      - "🚀 节点选择"
      - "香港"
      - "美国"
      - "日本"
      - "新加坡"
      - "🔧 手动切换"

  - name: "📹 YouTube"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/YouTube.png
    proxies:
      - "🚀 节点选择"
      - "香港"
      - "日本"
      - "新加坡"
      - "美国"
      - "🔧 手动切换"

  - name: "📲 Telegram"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Telegram.png
    proxies:
      - "🚀 节点选择"
      - "新加坡"
      - "美国"
      - "香港"
      - "日本"
      - "🔧 手动切换"

  # 兜底：未匹配流量（白名单模式 → 走代理）
  - name: "🐟 漏网之鱼"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Final.png
    proxies:
      - "🚀 节点选择"
      - "⚡ 自动选择"
      - "DIRECT"

  # ── 工具组 ──

  - name: "🔧 手动切换"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Static.png
    use:
      - MyAirport

  # ── 地区组（手动选择，默认自动测速最快节点）──

  - name: "香港"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Hong_Kong.png
    proxies:
      - "香港⚡"
    use:
      - MyAirport
    filter: "(?i)(HK|Hong Kong|香港|HKG|🇭🇰)"

  - name: "台湾"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Taiwan.png
    proxies:
      - "台湾⚡"
    use:
      - MyAirport
    filter: "(?i)(TW|Taiwan|台湾|TWN|🇹🇼)"

  - name: "日本"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Japan.png
    proxies:
      - "日本⚡"
    use:
      - MyAirport
    filter: "(?i)(JP|Japan|日本|JPN|Tokyo|Osaka|🇯🇵)"

  - name: "新加坡"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Singapore.png
    proxies:
      - "新加坡⚡"
    use:
      - MyAirport
    filter: "(?i)(SG|Singapore|新加坡|SGP|🇸🇬)"

  - name: "美国"
    type: select
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/United_States.png
    proxies:
      - "美国⚡"
    use:
      - MyAirport
    filter: "(?i)(US|United States|美国|USA|🇺🇸)"

  # ── 地区自动测速（内部组，被上方地区组引用）──

  - name: "香港⚡"
    type: url-test
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Hong_Kong.png
    use:
      - MyAirport
    filter: "(?i)(HK|Hong Kong|香港|HKG|🇭🇰)"
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    tolerance: 80
    lazy: true

  - name: "台湾⚡"
    type: url-test
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Taiwan.png
    use:
      - MyAirport
    filter: "(?i)(TW|Taiwan|台湾|TWN|🇹🇼)"
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    tolerance: 80
    lazy: true

  - name: "日本⚡"
    type: url-test
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Japan.png
    use:
      - MyAirport
    filter: "(?i)(JP|Japan|日本|JPN|Tokyo|Osaka|🇯🇵)"
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    tolerance: 80
    lazy: true

  - name: "新加坡⚡"
    type: url-test
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/Singapore.png
    use:
      - MyAirport
    filter: "(?i)(SG|Singapore|新加坡|SGP|🇸🇬)"
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    tolerance: 80
    lazy: true

  - name: "美国⚡"
    type: url-test
    icon: https://raw.githubusercontent.com/Koolson/Qure/master/IconSet/Color/United_States.png
    use:
      - MyAirport
    filter: "(?i)(US|United States|美国|USA|🇺🇸)"
    url: "http://www.gstatic.com/generate_204"
    interval: 300
    tolerance: 80
    lazy: true

# ─── 规则提供者 ────────────────────────────────────────────
# 仅保留 GEOSITE 无法覆盖的特殊规则集，其余全部用内置 GEOSITE 替代
# 优势：减少启动下载数、加快匹配速度、降低维护成本
rule-providers:

  # AI 服务（覆盖 OpenAI / Claude / Gemini / Copilot / Perplexity 等）
  # GEOSITE 的 openai 分类不够全面，使用第三方维护的综合列表
  ai_service:
    type: http
    behavior: classical
    format: text
    url: "https://raw.githubusercontent.com/EAlyce/conf/refs/heads/main/Rule/OpenAI.list"
    path: ./ruleset/ai_service.list
    interval: 86400

  # Adobe 正版验证屏蔽
  adobe_block:
    type: http
    behavior: classical
    url: "https://raw.githubusercontent.com/ignaciocastro/a-dove-is-dumb/refs/heads/main/clash.yaml"
    path: ./ruleset/adobe_block.yaml
    interval: 86400

  # Apple 国内 CDN（确保 Apple 中国服务走最快的国内节点）
  sukka_apple_cn:
    type: http
    behavior: classical
    format: text
    url: "https://ruleset.skk.moe/Clash/non_ip/apple_cn.txt"
    path: ./ruleset/sukka_apple_cn.txt
    interval: 43200

  # Microsoft 国内 CDN
  sukka_microsoft_cdn:
    type: http
    behavior: classical
    format: text
    url: "https://ruleset.skk.moe/Clash/non_ip/microsoft_cdn.txt"
    path: ./ruleset/sukka_microsoft_cdn.txt
    interval: 43200

  # 大文件下载直连（Windows Update、驱动、IDE 等）
  sukka_download:
    type: http
    behavior: domain
    format: text
    url: "https://ruleset.skk.moe/Clash/domainset/download.txt"
    path: ./ruleset/sukka_download.txt
    interval: 43200

  # 本地应用进程直连
  loyalsoldier_applications:
    type: http
    behavior: classical
    url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/applications.txt"
    path: ./ruleset/loyalsoldier_applications.txt
    interval: 86400

# ─── 分流规则 ──────────────────────────────────────────────
# 匹配顺序：上→下，首次命中即停止
# 设计原则：进程 → 特殊直连 → 屏蔽 → 国内CDN → 代理服务 → GFW兜底
#           → Apple/Microsoft直连 → 国内域名 → 国内IP → 白名单兜底
rules:
  # ─① 本地应用直连（进程级，最先处理）─
  - RULE-SET,loyalsoldier_applications,DIRECT

  # ─② Dashboard 面板直连 ─
  - DOMAIN,clash.razord.top,DIRECT
  - DOMAIN,yacd.haishan.me,DIRECT
  - DOMAIN,d.metacubex.one,DIRECT

  # ─③ 私有网络 / 局域网 ─
  - GEOSITE,private,DIRECT
  - GEOIP,private,DIRECT,no-resolve

  # ─④ 屏蔽 ─
  - RULE-SET,adobe_block,REJECT

  # ─⑤ 国内 CDN / 下载加速（范围精确，安全前置）─
  - RULE-SET,sukka_apple_cn,DIRECT
  - RULE-SET,sukka_microsoft_cdn,DIRECT
  - RULE-SET,sukka_download,DIRECT
  - GEOSITE,steam@cn,DIRECT
  - GEOSITE,category-games@cn,DIRECT

  # ─⑥ 代理服务（细粒度 → 粗粒度）─
  - RULE-SET,ai_service,🧠 AI 服务
  # OpenAI / ChatGPT（GEOSITE 兜底 + 显式域名，防 rule-set 下载失败）
  - GEOSITE,openai,🧠 AI 服务
  - DOMAIN-SUFFIX,chatgpt.com,🧠 AI 服务
  - DOMAIN-SUFFIX,oaistatic.com,🧠 AI 服务
  - DOMAIN-SUFFIX,oaiusercontent.com,🧠 AI 服务
  # Reddit
  - GEOSITE,reddit,🚀 节点选择
  - DOMAIN-SUFFIX,reddit.com,🚀 节点选择
  - DOMAIN-SUFFIX,redd.it,🚀 节点选择
  - DOMAIN-SUFFIX,redditmedia.com,🚀 节点选择
  - DOMAIN-SUFFIX,redditstatic.com,🚀 节点选择
  - GEOSITE,youtube,📹 YouTube
  - GEOSITE,google,🔍 Google
  - GEOSITE,telegram,📲 Telegram
  - GEOSITE,github,🚀 节点选择
  - GEOSITE,twitter,🚀 节点选择
  - GEOSITE,category-scholar-!cn,🚀 节点选择

  # ─⑦ Apple & Microsoft 全球服务直连 ─
  # 中国可正常访问，默认直连；如需代理请使用 advanced 配置
  - GEOSITE,apple,DIRECT
  - GEOSITE,microsoft,DIRECT

  # ─⑧ GFW 域名兜底 ─
  - GEOSITE,gfw,🚀 节点选择

  # ─⑨ 国内域名 & IP 兜底 ─
  - GEOSITE,cn,DIRECT
  - GEOIP,CN,DIRECT,no-resolve

  # ─⑩ 最终兜底（白名单模式：未知流量 → 代理，确保安全）─
  - MATCH,🐟 漏网之鱼